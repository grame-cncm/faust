--- a/arch/cpp/bench.h
+++ b/arch/cpp/bench.h
@@ -18,6 +18,9 @@
 #ifndef BENCH_CASE
 #define BENCH_CASE "default"
 #endif
+#ifndef BENCH_MODE
+#define BENCH_MODE "scalar"
+#endif
 #ifndef BENCH_OPTIM
 #define BENCH_OPTIM "O3"
 #endif
@@ -206,6 +209,7 @@
     vstring language          = BENCH_LANG;
     vstring dsp               = BENCH_DSP;
     vstring bench_case        = BENCH_CASE;
+    vstring bench_mode        = BENCH_MODE;
     vstring precision         = PRECISION;
     vstring opt               = BENCH_OPTIM;
     s32 samp_rate             = SAMP_RATE;
@@ -325,6 +329,7 @@
     printf("  language:       %s\n",     report.language.data());
     printf("  dsp:            %s\n",     report.dsp.data());
     printf("  bench case:     %s\n",     report.bench_case.data());
+    printf("  bench mode:     %s\n",     report.bench_mode.data());
     printf("  precision:      %s\n",     report.precision.data());
     printf("  optimization:   %s\n",     report.opt.data());
     puts("------------------------------------");
@@ -373,10 +378,10 @@
 
     fprintf(
         fp,
-        "%s,%s,%s,%s,%d,%d,%d,%d,%d,%d,%d,%.17g,%.17g,%.17g,%.17g,%.17g,"
+        "%s,%s,%s,%s,%s,%d,%d,%d,%d,%d,%d,%d,%.17g,%.17g,%.17g,%.17g,%.17g,"
         "%.17g,%.17g,%.17g,%.17g,%.17g,%.17g,%.17g,%.17g,%.17g,%.17g\n",
         report.language.data(), report.bench_case.data(),
-        report.precision.data(), report.opt.data(),
+        report.bench_mode.data(), report.precision.data(), report.opt.data(),
         report.samp_rate, report.buff_size,
         report.inputs, report.outputs,
         report.warmup_iters, report.run_iters, report.batches,
--- a/arch/mojo/bench.mojo
+++ b/arch/mojo/bench.mojo
@@ -17,6 +17,7 @@
 comptime BENCH_LANG = get_defined_string["BENCH_LANG", "mojo"]()
 comptime BENCH_DSP = get_defined_string["BENCH_DSP", "unknown"]()
 comptime BENCH_CASE = get_defined_string["BENCH_CASE", "default"]()
+comptime BENCH_MODE = get_defined_string["BENCH_MODE", "scalar"]()
 comptime BENCH_OPTIM = get_defined_string["BENCH_OPTIM", "O3"]()
 comptime BENCH_BARRIERS = get_defined_int["BENCH_BARRIERS", 1]()
 comptime WARMUP_ITERS = get_defined_int["WARMUP_ITERS", 50]()
@@ -71,6 +72,7 @@
     var language: String
     var dsp: String
     var bench_case: String
+    var bench_mode: String
     var precision: String
     var opt: String
     var samp_rate: S32
@@ -99,6 +101,7 @@
         report.language = BENCH_LANG
         report.dsp = BENCH_DSP
         report.bench_case = BENCH_CASE
+        report.bench_mode = BENCH_MODE
         report.precision = PRECISION
         report.opt = BENCH_OPTIM
         report.samp_rate = SAMP_RATE
@@ -302,6 +305,7 @@
     print("  language:       ", report.language)
     print("  dsp:            ", report.dsp)
     print("  bench case:     ", report.bench_case)
+    print("  bench mode:     ", report.bench_mode)
     print("  precision:      ", report.precision)
     print("  optimization:   ", report.opt)
     print("------------------------------------")
@@ -341,7 +345,8 @@
     # Does not write headers or manage existing CSV data.
     # Called only for structured runs when WRITE_CSV is enabled.
     var csv = String(
-        report.language + "," + report.bench_case + "," + report.precision + "," + report.opt + ","
+        report.language + "," + report.bench_case + "," + report.bench_mode + ","
+        + report.precision + "," + report.opt + ","
         + String(report.samp_rate) + "," + String(report.buff_size) + ","
         + String(report.inputs) + "," + String(report.outputs) + ","
         + String(report.warmup_iters) + "," + String(report.run_iters) + ","
--- a/script/csvmanip.py
+++ b/script/csvmanip.py
@@ -9,6 +9,7 @@
 TMP_COLUMNS = [
     "language",
     "bench_case",
+    "bench_mode",
     "precision",
     "opt",
     "samp_rate",
@@ -39,6 +40,7 @@
     "language",
     "dsp",
     "bench_case",
+    "bench_mode",
     "precision",
     "opt",
     "samp_rate",
@@ -72,6 +74,10 @@
 
     df = pd.read_csv(path)
 
+    if "bench_mode" not in df.columns and "bench_case" in df.columns:
+        insert_at = list(df.columns).index("bench_case") + 1
+        df.insert(insert_at, "bench_mode", "scalar")
+
     missing = [col for col in CSV_COLUMNS if col not in df.columns]
     if missing:
         raise SystemExit(f"main CSV is missing columns: {missing}")
@@ -96,7 +102,14 @@
     return df[CSV_COLUMNS]
 
 
-def merge_csv(main_path: Path, tmp_path: Path, language: str, dsp: str, bench_case: str) -> None:
+def merge_csv(
+    main_path: Path,
+    tmp_path: Path,
+    language: str,
+    dsp: str,
+    bench_case: str,
+    bench_mode: str | None = None,
+) -> None:
     main_df = read_main_csv(main_path)
     tmp_df = read_tmp_csv(tmp_path, dsp)
 
@@ -113,10 +126,18 @@
                 f"temporary CSV does not match expected {col}={value}: found {sorted(values)}"
             )
 
+    tmp_modes = set(str(x) for x in tmp_df["bench_mode"].unique())
+    if bench_mode is not None and tmp_modes != {bench_mode}:
+        raise SystemExit(
+            f"temporary CSV does not match expected bench_mode={bench_mode}: "
+            f"found {sorted(tmp_modes)}"
+        )
+
     keep_mask = ~(
         (main_df["language"].astype(str) == language)
         & (main_df["dsp"].astype(str) == dsp)
         & (main_df["bench_case"].astype(str) == bench_case)
+        & (main_df["bench_mode"].astype(str).isin(tmp_modes))
     )
 
     out_df = pd.concat(
@@ -131,6 +152,7 @@
         "dsp",
         "language",
         "bench_case",
+        "bench_mode",
         "precision",
         "opt",
         "samp_rate",
@@ -143,7 +165,8 @@
 
     print(
         f"merged {tmp_path} into {main_path} "
-        f"for language={language}, dsp={dsp}, bench_case={bench_case}"
+        f"for language={language}, dsp={dsp}, bench_case={bench_case}, "
+        f"bench_mode={','.join(sorted(tmp_modes))}"
     )
 
 
@@ -163,6 +186,7 @@
     merge.add_argument("--language", required=True, help="Benchmark language.")
     merge.add_argument("--dsp", required=True, help="DSP benchmark name.")
     merge.add_argument("--case", required=True, help="Benchmark case.")
+    merge.add_argument("--mode", default=None, help="Optional benchmark mode.")
 
     args = parser.parse_args()
 
@@ -173,6 +197,7 @@
             language=args.language,
             dsp=args.dsp,
             bench_case=args.case,
+            bench_mode=args.mode,
         )
 
 
