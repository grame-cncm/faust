//----------------------------------------------------------
// name: "highShelf"
//
// Code generated with Faust 2.79.0 (https://faust.grame.fr)
// Compiler options: -lang ocpp -ct 1 -es 0 -mcl 4 -mcd 9 -mfs 1024 -huf 0 -irt 4 -fls 4 -udd 1 -mdd 1024 -mdy 90 -mca 8 -ss 0 -single -ftz 0 
//----------------------------------------------------------

/* link with  */
#include <algorithm>
#include <cmath>
#include <math.h>
#ifndef FAUSTFLOAT
#define FAUSTFLOAT float
#endif 


#ifndef FAUSTCLASS 
#define FAUSTCLASS mydsp
#endif

class mydsp : public dsp {
  private:
	FAUSTFLOAT 	fslider0;
	FAUSTFLOAT 	fslider1;
	FAUSTFLOAT 	fslider2;
	float 	fConst0KI; // step: 29
	// Recursion delay fVec1SE is of type kSingleDelay
	// While its definition is of type kZeroDelay
	float 	fVec1SEState; // Single Delay
	float 	fVec0SEState[2]; // Copy Delay
	int fSampleRate;

  public:
	virtual void metadata(Meta* m) { 
		m->declare("compile_options", "-lang ocpp -ct 1 -es 0 -mcl 4 -mcd 9 -mfs 1024 -huf 0 -irt 4 -fls 4 -udd 1 -mdd 1024 -mdy 90 -mca 8 -ss 0 -single -ftz 0");
		m->declare("filename", "highShelf.dsp");
		m->declare("math.lib/author", "GRAME");
		m->declare("math.lib/copyright", "GRAME");
		m->declare("math.lib/deprecated", "This library is deprecated and is not maintained anymore. It will be removed in August 2017.");
		m->declare("math.lib/license", "LGPL with exception");
		m->declare("math.lib/name", "Math Library");
		m->declare("math.lib/version", "1.0");
		m->declare("maxmsp.lib/author", "GRAME");
		m->declare("maxmsp.lib/copyright", "GRAME");
		m->declare("maxmsp.lib/license", "LGPL");
		m->declare("maxmsp.lib/name", "MaxMSP compatibility Library");
		m->declare("maxmsp.lib/version", "1.1");
		m->declare("music.lib/author", "GRAME");
		m->declare("music.lib/copyright", "GRAME");
		m->declare("music.lib/license", "LGPL with exception");
		m->declare("music.lib/name", "Music Library");
		m->declare("music.lib/version", "1.0");
		m->declare("name", "highShelf");
	}

	virtual int getNumInputs() { return 1; }
	virtual int getNumOutputs() { return 1; }
	static void classInit(int sample_rate) {
	}
	virtual void instanceConstants(int sample_rate) {
		fSampleRate = sample_rate;
		fConst0KI = (6.2831855f / std::min(1.92e+05f, std::max(1.0f, float(fSampleRate)))); // step: 29
	}
	virtual void instanceResetUserInterface() {
		fslider0 = 0.0f;
		fslider1 = 1.0f;
		fslider2 = 1e+03f;
	}
	virtual void instanceClear() {
		fVec1SEState = 0;
		for (int j = 0; j < 2; j++) { fVec0SEState[j] = 0; }
	}
	virtual void init(int sample_rate) {
		classInit(sample_rate);
		instanceInit(sample_rate);
	}
	virtual void instanceInit(int sample_rate) {
		instanceConstants(sample_rate);
		instanceResetUserInterface();
		instanceClear();
	}
	virtual mydsp* clone() {
		return new mydsp();
	}
	virtual int getSampleRate() {
		return fSampleRate;
	}
	virtual void buildUserInterface(UI* ui_interface) {
		ui_interface->openVerticalBox("highShelf");
		ui_interface->addHorizontalSlider("Freq", &fslider2, 1e+03f, 1e+02f, 1e+04f, 1.0f);
		ui_interface->declare(&fslider0, "unit", "dB");
		ui_interface->addHorizontalSlider("Gain", &fslider0, 0.0f, -1e+01f, 1e+01f, 0.1f);
		ui_interface->addHorizontalSlider("Q", &fslider1, 1.0f, 0.01f, 1e+02f, 0.01f);
		ui_interface->closeBox();
	}
	virtual void compute (int count, FAUSTFLOAT** input, FAUSTFLOAT** output) {
		float 	fSlow0BE = powf(1e+01f,(0.025f * float(fslider0))); // Zone 2, step: 12
		float 	fSlow1BE = (fConst0KI * std::max(0.0f, float(fslider2))); // Zone 2, step: 30
		float 	fSlow2BE = ((sqrtf(fSlow0BE) * sinf(fSlow1BE)) / std::max(0.001f, float(fslider1))); // Zone 2, step: 33
		float 	fSlow3BE = cosf(fSlow1BE); // Zone 2, step: 34
		float 	fSlow4BE = (fSlow0BE + -1.0f /* Sum */); // Zone 2, step: 36
		float 	fSlow5BE = (fSlow3BE * fSlow4BE); // Zone 2, step: 37
		float 	fSlow6BE = (fSlow0BE + fSlow2BE + 1.0f + -fSlow5BE /* Sum */); // Zone 2, step: 39
		float 	fSlow7BE = (1.0f / fSlow6BE); // Zone 2, step: 40
		float 	fSlow8BE = (fSlow0BE + 1.0f + -fSlow5BE + -fSlow2BE /* Sum */); // Zone 2, step: 42
		float 	fTemp0SE; // step: 44
		float 	fSlow9BE = (fSlow3BE * (fSlow0BE + 1.0f /* Sum */)); // Zone 2, step: 48
		float 	fSlow10BE = (2.0f * (fSlow0BE + -1.0f + -fSlow9BE /* Sum */)); // Zone 2, step: 51
		float 	fVec1SE[2];
		float 	fTemp1SE; // step: 61
		float 	fVec0SE[3];
		float 	fSlow11BE = (fSlow0BE / fSlow6BE); // Zone 2, step: 62
		float 	fSlow12BE = (fSlow4BE * fSlow3BE); // Zone 2, step: 63
		float 	fSlow13BE = (fSlow0BE + fSlow12BE + fSlow2BE + 1.0f /* Sum */); // Zone 2, step: 64
		float 	fSlow14BE = (2.0f * (fSlow0BE + fSlow9BE + -1.0f /* Sum */)); // Zone 2, step: 70
		float 	fSlow15BE = (fSlow0BE + fSlow12BE + 1.0f + -fSlow2BE /* Sum */); // Zone 2, step: 73
		FAUSTFLOAT* input0 = input[0]; // Zone 3
		FAUSTFLOAT* output0 = output[0]; // Zone 3
		fVec1SE[1] = fVec1SEState;
		fVec0SE[1] = fVec0SEState[0];
		fVec0SE[2] = fVec0SEState[1];
		for (int i=0; i<count; i++) {
			fTemp0SE = fVec0SE[2]; // step: 44
			fVec1SE[0] = ((float)input0[i] + -(fSlow7BE * ((fSlow8BE * fTemp0SE) + (fSlow10BE * fVec1SE[1]) /* Sum */)) /* Sum */);
			fTemp1SE = fVec1SE[0]; // step: 61
			fVec0SE[0] = fTemp1SE;
			output0[i] = (FAUSTFLOAT)((fSlow11BE * ((fSlow13BE * fVec1SE[0]) + -(fSlow14BE * fVec0SE[1]) + (fSlow15BE * fTemp0SE) /* Sum */)));  // Zone Exec Code
			// post processing
			fVec0SE[2] = fVec0SE[1];
			fVec0SE[1] = fVec0SE[0];
			fVec1SE[1] = fVec1SE[0];
		}
		fVec1SEState = fVec1SE[1];
		fVec0SEState[0] = fVec0SE[1];
		fVec0SEState[1] = fVec0SE[2];
	}
};


